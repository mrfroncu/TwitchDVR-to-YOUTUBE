# Copy to .env next to docker-compose.yml (both are gitignored / never deployed)
# Password required to open the web UI (HTTP Basic auth; any username works).
# Leave empty to disable authentication (only do that on a trusted network!).
WEB_PASSWORD=change-me
